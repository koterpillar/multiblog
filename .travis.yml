language: haskell

ghc:
  - 7.8

before_install:
  - cabal sandbox init
  - cabal install hlint

install:
  - cabal configure --enable-tests
  - cabal install --only-dependencies
  - cabal build

script:
  - cabal test

cache:
  directories:
    - .cabal-sandbox

sudo: false
